The basis of this lies in considering the coordinates, as defined as a in a mathematical matrix. Where the [i,j]'th element, refers to the i'th row and j'th column

As well as the HTML elements corresponding to these coordiantes, which we will refer to as a 'cell'

Note API key is blanked out from public github depository


key features of server to remeber:

*change to .mjs, and change sytnax to "import express from 'express';
import fetch from 'node-fetch';

*import fetch function

*use "app.use(express.json());" to import bodies 


*have to change API urls depending on hosting platform or on local environment

*remember to install the random-words package